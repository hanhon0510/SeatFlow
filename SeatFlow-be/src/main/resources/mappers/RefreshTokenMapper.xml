<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.seatflow.auth.RefreshTokenMapper">
    <resultMap id="RefreshTokenRecordResultMap" type="com.seatflow.auth.RefreshTokenRecord">
        <constructor>
            <idArg column="id" javaType="java.util.UUID" />
            <arg column="user_id" javaType="java.util.UUID" />
            <arg column="token_hash" javaType="java.lang.String" />
            <arg column="expires_at" javaType="java.time.Instant" />
            <arg column="revoked_at" javaType="java.time.Instant" />
            <arg column="created_at" javaType="java.time.Instant" />
        </constructor>
    </resultMap>

    <insert id="insert" parameterType="com.seatflow.auth.RefreshTokenRecord">
        INSERT INTO refresh_tokens (
            id,
            user_id,
            token_hash,
            expires_at
        )
        VALUES (
            #{id},
            #{userId},
            #{tokenHash},
            #{expiresAt}
        )
    </insert>

    <select id="findActiveByHash" resultMap="RefreshTokenRecordResultMap">
        SELECT
            id,
            user_id,
            token_hash,
            expires_at,
            revoked_at,
            created_at
        FROM refresh_tokens
        WHERE token_hash = #{tokenHash}
          AND revoked_at IS NULL
          AND expires_at > #{now}
    </select>

    <select id="findByHash" resultMap="RefreshTokenRecordResultMap">
        SELECT
            id,
            user_id,
            token_hash,
            expires_at,
            revoked_at,
            created_at
        FROM refresh_tokens
        WHERE token_hash = #{tokenHash}
    </select>

    <update id="revoke">
        UPDATE refresh_tokens
        SET revoked_at = #{revokedAt}
        WHERE id = #{id}
          AND revoked_at IS NULL
    </update>

    <update id="revokeAllUserTokens">
        UPDATE refresh_tokens
        SET revoked_at = #{revokedAt}
        WHERE user_id = #{userId}
          AND revoked_at IS NULL
    </update>

    <delete id="deleteExpired">
        DELETE FROM refresh_tokens
        WHERE expires_at &lt;= #{now}
    </delete>
</mapper>
