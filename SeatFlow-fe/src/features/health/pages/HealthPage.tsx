import { Typography } from 'antd'

export function HealthPage() {
  return (
    <section className="page-surface">
      <Typography.Title level={1}>SeatFlow frontend is running.</Typography.Title>
    </section>
  )
}
